"""Backtesting (PLAN §6): event-driven multi-day replay over the shared core + metrics.

Exports are wired during integration; submodules are imported directly by their owners
to keep package import light during parallel development.
"""
