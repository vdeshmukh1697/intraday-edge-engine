# Base URL of the FastAPI signal engine. No trailing slash.
NEXT_PUBLIC_API_BASE=http://127.0.0.1:8000

# Optional API key. When set, it is sent as the X-API-Key header on every request.
# Must match the engine's SE_API_TOKEN.
NEXT_PUBLIC_API_KEY=
